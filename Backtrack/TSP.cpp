#include <bits/stdc++.h>
using namespace	std;

/* Cho trước một danh sách các thành phố 
và khoảng cách giữa chúng, tìm chu trình 
ngắn nhất thăm mỗi thành phố đúng một lần. */

int* reducedArr(int n ; int a[][]){

}

void solve(int n ; int a[][]){

}

int main(){
	int TC; cin >> TC;
	while(TC--){
		int n; cin >> n;
		int a[n][n];
		for(int i = 0; i < n; i++){
			for(int j = 0; j < n; j++){
				cin >> a[i][j];
			}
		}
		solve(n,a);
	}

	return 0;
}